export const ANSWER = document.querySelector("#answer") as HTMLElement;
export const LETTERS = document.querySelector("#letters") as HTMLElement;
export const CONTAINER = document.querySelector(".container") as HTMLElement;
export const TOTAL_QUESTIONS = document.querySelector(
  "#total_questions"
) as HTMLElement;
export const CURRENT_QUESTION = document.querySelector(
  "#current_question"
) as HTMLElement;

export const ERRORS_LIMIT = 3;
export const COUNTER_UNIT = 1;
export const TASKS_NUMBER = 6;
export const COUNT_INITIAL_VAL = 0;
export const PROCESSING_TIMEOUT = 1800;
export const COMMON_TIMEOUT = 1500;
